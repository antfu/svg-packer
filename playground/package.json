{
  "name": "playground",
  "type": "module",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "dev": "vite",
    "build": "tsc && vite build",
    "preview": "vite preview",
    "test": "vitest",
    "test:headless": "CI=true vitest run",
    "test:ci": "vitest run"
  },
  "dependencies": {
    "svg-packer": "workspace:*"
  },
  "devDependencies": {
    "vite-plugin-node-polyfills": "^0.23.0"
  }
}
